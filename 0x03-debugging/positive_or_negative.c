#include <stdio.h>
#include "main.h"

void positive_or_negative(int i)
{
	if (i > 0)
		printf("%d is positive\n", f);
	else if (i == 0)
		printf("%d is zero\n", f);
	else
		printf("%d is negative\n", f);
} 
